openai>=1.30.0
pydantic>=2.5.0
pyyaml>=6.0
mcp>=2.0.0

# 可观测性引擎依赖（必选）
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0

# 可选依赖：tiktoken 用于精确 token 计数，不安装时自动回退到字符启发式估算
tiktoken>=0.7.0
